# DB Redis Cache
    database redis

# Version last
    last

 # Build
  > docker-compose up -d    

# Build
    docker-compose up -d

    docker start/stop cache_redis
    docker start/stop cache_redis 