# DB Redis Cache
    database redis

# Version last
    last


# Build   
``` powershell  
    $ docker-compose up -d

    $ docker start/stop cache_redis
    
    $ docker start/stop cache_redis
```    